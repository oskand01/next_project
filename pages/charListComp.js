


function showCharacters({allCharacters}){
    console.log(allCharacters);
    return(
        <>
        <div>
            <h1>test</h1>
        </div>
        </>
    )
//need to add a function that gets all characters default and prints them but need to check so that the prop is not empty if so i need to check the other prop if data has been sent from search function
}
export default showCharacters





